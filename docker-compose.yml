services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: propertyops
    ports:
      - "5432:5432"
    volumes:
      - propertyops_db:/var/lib/postgresql/data

volumes:
  propertyops_db:
